# Complete Source Snapshot

Place the supplied final project archive here:

`NoC_Router_Fixed_4x4mesh_4cluster_final.zip`

This archive is the complete source snapshot used to assemble the public showcase repository. It contains the full RTL and testbench set, including legacy/experimental variants retained for traceability.

The browsable `Source/RTL/` directory is reserved for the key modules that are most useful to a reviewer, while the archive preserves the complete project snapshot without silently dropping files.
